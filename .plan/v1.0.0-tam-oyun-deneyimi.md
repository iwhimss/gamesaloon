# v1.0.0 — Tam Oyun Deneyimi (Lobi → Okey → Puan → PWA → Deploy Hazırlığı → Tasarım)

Durum: **PLANLAMA TAMAMLANDI — kodlamaya onay verildi, uygulanıyor**
Önceki sürüm: [`v0.0.1-kurulum.md`](v0.0.1-kurulum.md) (tamamlandı, GitHub'da)
Kaynak plan: [`../oyun-platformu-proje-plani.md`](../oyun-platformu-proje-plani.md) — roadmap madde 3-10

## Kapsam

**Bu sürümde tamamlanacak (roadmap 3-8):**
- Lobi + masa (oda) sistemi — masa kurma/katılma, kod+şifre, oda listesi
- Host yetkileri — oyunu başlatma, oyuncu atma, ayar değiştirme, hostluk devri, otomatik devir
- Okey oyun motoru (backend, saf fonksiyonlar) — 106 taş, gösterge/okey belirleme, çekme/atma, el bitirme, temel puanlama
- Okey frontend arayüzü — oyun masası, taş görselleri, sıra göstergesi, sürükle-bırak
- Puan sistemi ve oturum sonu ekranları
- PWA (manifest, service worker, "ana ekrana ekle")
- **Kapsamlı tasarım/stil geçişi**: sade, göze hoş, büyük/net tipografi, yüksek kontrast — yaşlı kullanıcılar dahil herkesin kolayca kullanabileceği bir arayüz. Lobi, giriş, masa, oyun ekranlarının tamamına uygulanacak.

**Bu sürümde YAPILMAYACAK (kullanıcı onayıyla netleşti):**
- Gerçek VDS'e deploy (SSH erişimi yok) — sadece **hazırlık dosyaları** üretilecek: `docker-compose.prod.yml`, nginx reverse proxy config örneği, Certbot adımları, `.env.production.example`. Gerçek sunucuya yükleme kullanıcı tarafından yapılacak.
- Gerçek (şifreli) kullanıcı kaydı — misafir modu v0.0.1'de zaten var, bu sürümde dokunulmuyor.
- Minigame/güç kartları/emoji gibi v2+ özellikleri (orijinal plan dosyasında da net şekilde dışlanmış).

## Versiyon ve Push Stratejisi
- Her faz tamamlandıkça yerel commit yapılır ve bu dosya güncellenir.
- **Push**: tüm fazlar bitene kadar GitHub'a gönderilmez — kullanıcı, sonda tek seferlik onay verecek (önceki sürümden farklı, kullanıcı tercihi).
- Sürüm numarası: bu çalışmanın sonunda kök/backend/frontend `package.json` → `1.0.0`, `CHANGELOG.md`'ye v1.0.0 girdisi eklendi. ✅

## Son Doğrulama (2026-07-30)
- Tüm imajlar yeniden build edildi (`docker compose build`) ve `docker compose up -d` ile ayağa kaldırıldı.
- **Bug bulundu ve düzeltildi**: `/health` endpoint'i `process.env.npm_package_version` kullanıyordu — bu değişken sadece `npm run` ile başlatılan process'lerde set edilir, Docker container'ı backend'i doğrudan `node src/index.js` ile çalıştırdığı için prod'da her zaman `0.0.1` (hardcoded fallback) dönüyordu. Düzeltme: `backend/package.json`'ı `createRequire` ile doğrudan okuyup versiyonu oradan alıyor. Doğrulandı: `/health` artık `"version":"1.0.0"` dönüyor.
- **Not**: `docker compose build` sırasında frontend build'i bir kez npm registry ağ zaman aşımı (`EIDLETIMEOUT`) nedeniyle başarısız oldu; `docker compose up -d` bu durumda sessizce eski (stale) frontend imajını kullanmaya devam etti (build hatası `up`'ı durdurmadı). Frontend'i tekrar build edip (`docker compose build frontend`) güncel imajla değiştirdikten sonra tarayıcıda doğrulandı (yeni 🃏 başlık ve metinler göründü).
- Docker üzerinden misafir girişi + lobi akışı tarayıcıda doğrulandı.

---

## Faz A — Lobi ve Masa (Oda) Sistemi ✅ (2026-07-30)
- [x] DB: `tables`/`table_players` üzerinde CRUD (kod üretimi: 6 haneli alfanümerik — karışmaya sebep olan 0/O/1/I çıkarıldı, opsiyonel şifre bcrypt ile hash'lenir)
- [x] Socket events: `room:create`, `room:join`, `room:leave`, sunucu → `room:state`
- [x] REST: `GET /tables` (lobide açık/dolu masa listesi — dolu/boş, oyun tipi, oyuncu sayısı)
- [x] Masa durumları: `bekleniyor` → `oynanıyor` → `bitti/kapandı`
- [x] Aktif oda state'i Redis'te tutulur (`backend/src/rooms/roomStore.js`, TTL 12 saat); masa kapanınca `tables.status='kapandı'` + Redis key silinir
- [x] Frontend: Lobi ekranı (masa listesi, "Masa Kur" butonu, kod+şifre ile katılma formu) — `frontend/src/screens/LobbyScreen.jsx`, `TableScreen.jsx`
- **Uçtan uca test edildi** (2 farklı misafir, 2 tarayıcı sekmesi): masa kurma → lobide görünme → katılma → gerçek zamanlı `room:state` yayını her iki tarafta da doğru → host ayrılınca otomatik devir (Ayse host oldu) → son oyuncu da ayrılınca masa `kapandı` durumuna geçti (Postgres'te doğrulandı)
- Not: Redis'e host makinesinden erişim için docker-compose'a `6380:6379` port eşlemesi eklendi (6379 diğer bir projeyle çakışabiliyordu).

## Faz B — Host Yetkileri ✅ (2026-07-30, `host:start` Faz C ile birlikte yapılacak)
- [x] Masayı kuran otomatik host olur (`host_user_id`) — Faz A'da zaten var
- [x] Host aksiyonları: `host:kick`, `host:changePassword`, `host:transfer` (`host:start` → Faz C'de okey motoruyla birlikte eklenecek, `host:changeSettings` şu an sadece şifre kapsıyor, oyun tipi tek seçenek olduğu için gerek yok)
- [x] Host ayrılırsa otomatik en kıdemli oyuncuya devir (Faz A'da zaten vardı, doğrulandı)
- [x] Yetki kontrolü: host olmayan kullanıcıdan gelen host komutları reddedilir (ack callback ile `{ok:false, error}`)
- [x] Frontend: host'a özel kontrol paneli (masa içi: at/host yap/şifre değiştir), diğer oyunculara host rozeti gösterimi
- **Uçtan uca test edildi** (2 misafir): kick çalıştı ve atılan kullanıcı lobiye döndü, host devri çalıştı (kontroller doğru taraf değiştirdi), şifre güncelleme çalıştı (🔒 rozeti belirdi)
- **Kritik bug bulundu ve düzeltildi**: atılan/ayrılan bir kullanıcı aynı masaya tekrar katılınca `table_players` üzerinde `(table_id, user_id)` unique constraint çakışması backend'i **çökertiyordu** (yakalanmamış promise rejection). Düzeltme: `INSERT ... ON CONFLICT DO UPDATE` (upsert) + tüm oda/host socket handler'larına try/catch + `index.js`'e global `unhandledRejection` güvenlik ağı eklendi. Yeniden test edildi, sorun yok.

## Faz C — Okey Oyun Motoru (Backend, Saf Fonksiyonlar) ✅ (2026-07-30)
- [x] `backend/src/games/okey/` modülü: `createGame`, `handleAction`, `getStateForPlayer`, `isGameOver`, `calculateScore` (plan dosyasındaki plugin arayüzü)
- [x] 106 taş üretimi (1-13 x 4 renk x 2 takım + 2 sahte okey), karıştırma — `tiles.js`
- [x] Gösterge taşı çekme, okey taşı belirleme (sahte okey indikatör olamaz, tekrar çekilir) — `rules.js: computeOkeyTile`
- [x] Sırayla çekme (orta yığın/atılan taş) ve atma — `engine.js: handleAction`
- [x] El bitirme kontrolü: çift (7 çift + 1 fazla) ve seri/grup (5x3 set/run) kombinasyonları — `rules.js: isWinningHand`, joker/okey ikamesi dahil
- [x] Temel puanlama: elini bitiren +10×(kaybeden sayısı), her kaybeden -10 (zero-sum) — `engine.js: calculateScore`
- [x] **Saf fonksiyon prensibi**: `rules.js`/`engine.js` state içeri, yeni state dışarı, socket'e bağımlı değil — `rules.test.manual.mjs` ile 6 senaryo doğrulandı (`npm run test:okey-rules`)
- [x] Socket entegrasyonu: `game:start` (host-only), `game:action` ({draw|discard|finishHand}), sunucu → `game:state` (SADECE kendi eli, per-socket), `game:handEnded`
- [x] Güvenlik: `getStateForPlayer` ile per-socket filtrelenmiş state yayını — rakip eli asla client'a sızdırılmaz (sadece `tileCount` görünür)
- Bonus (Faz E'den öne alındı): el bitince `game_sessions`/`score_history`'ye otomatik yazım (`gameHandlers.js: persistHandResult`)
- Not: `rules.js` validator'ı klasik Rummikub-tipi backtracking algoritması kullanıyor (anchor-tile + set/run deneme + joker ikamesi); 6 manuel test senaryosuyla doğrulandı ama gerçek oyunla da uçtan uca test edilecek (Faz D).

## Faz D — Okey Frontend Arayüzü ✅ (2026-07-30)
- [x] Oyun masası bileşeni: oyuncu listesi + sıra vurgusu, orta yığın (kalan taş sayısı), atılan taş yığını (üstteki taş) — `GameScreen.jsx`
- [x] Taş görselleri: CSS tabanlı, renk (kırmızı/sarı/mavi/siyah) + numara, sahte okey `★` sembolü — `components/Tile.jsx`, görsel varlık gerektirmez
- [x] Kendi elini göster; **tasarım kararı**: sürükle-bırak yerine tıkla-seç + "Seçili taşı at" butonu kullanıldı (erişilebilirlik/yaşlı kullanıcı hedefi ile daha uyumlu — Faz F'nin ruhuna uygun bilinçli sapma, orijinal plandaki drag-drop yerine)
- [x] Sıra göstergesi: aktif oyuncu satırı vurgulanıyor + "Sırası" rozeti
- [x] El bitirme/kazanma bildirimi: `HandEndScreen.jsx` (kazanan, puan değişimleri, berabere durumu)
- **Uçtan uca test edildi** (4 gerçek misafir, 4 tarayıcı sekmesi): oyun başlatma (14 taş dağıtımı), çekme (yığın + atılandan), atma, sıra ilerlemesi, **güvenlik doğrulandı** (rakip elleri asla görünmüyor, sadece taş sayısı), joker (★) doğru render, elini bitirme → puanlama → el sonu ekranı → masaya dönüş tam döngü çalıştı
- **Bug bulundu ve düzeltildi**: `createGame`'de gösterge taşı sahte-okey çıkarsa yeniden çekiliyordu ama reddedilen taş desteye geri konmuyordu — 106 taş yerine 105 ile oynanıyordu (49 yerine 48 taş kalıyordu). Düzeltme: reddedilen taş `deck.unshift()` ile desteye geri konuyor. Gerçek oyunda gösterge=13 siyah → okey=1 siyah (sarma) doğru hesaplandı, doğrulandı.

## Faz E — Puan Sistemi ve Oturum Sonu ✅ (2026-07-30)
- [x] Her el sonunda skor tablosu güncellenir (masa içi, oturuma özel `sessionScores` state, Redis'te tutuluyor)
- [x] `game_sessions`/`score_history`'ye her el bitince otomatik yazılır (masa kapanmasını beklemeden — veri kaybı riskini azaltır, Faz C'de zaten yapılmıştı)
- [x] Frontend: el sonu ekranı (kazanan, el puan değişimleri) + oturum toplamı — `HandEndScreen.jsx`
- [x] TableScreen'de kalıcı "Oturum skor tablosu (N el oynandı)" bölümü, host için "Yeni El Başlat" butonu (birden fazla el arka arkaya oynanabilir)
- **Uçtan uca test edildi** (4 misafir, 1 el): el bitirme → puan dağılımı (+30/-10/-10/-10) → oturum toplamı doğru → masaya dönünce TableScreen'de skor tablosu doğru gösterildi → DB'de `game_sessions.result_json` ve `score_history` satırları doğrulandı

## Faz F — Tasarım / Stil Geçişi (tüm ekranlara uygulanır) ✅ (2026-07-30)
- [x] Ortak tasarım sistemi: CSS custom properties (renk paleti — sıcak/nötr ton `#f6f4ee` arka plan, yüksek kontrast metin, mavi vurgu), taban font boyutu 18px (mobilde 16px), tutarlı radius/gölge/boşluk
- [x] Bileşenler: buton (min 48px yükseklik — dokunma hedefi), input (min 48px), kart, rozet (host, bağlantı durumu) tek bir stil dilinde
- [x] Erişilebilirlik: `:focus-visible` ile net klavye odak halkası, buton/input min. dokunma alanı, sade Türkçe metinler (zaten mevcuttu)
- [x] Misafir giriş (🃏 rozeti eklendi), lobi, masa, oyun masası, el/oturum sonu ekranlarının tamamı ortak `App.css` token sistemiyle stillendirildi
- [x] Responsive: `@media (max-width: 640px)` ile mobilde font/padding küçültme, flex-wrap ile taşma önlendi
- Not: JSX/class isimleri değişmedi (sadece CSS token'ları), tarayıcıda hızlı doğrulama yapıldı (konsol hatası yok, sayfa render doğru). Ekran görüntüsü bu ortamda alınamadı (Browser pane inaktif) — kullanıcı canlı tarayıcıda görsel olarak kontrol edebilir.

## Faz G — PWA ✅ (2026-07-30)
- [x] `manifest.json` (isim, SVG ikon, tema rengi `#1d4ed8`, `display: standalone`)
- [x] Service worker (`public/sw.js`): navigasyonlarda önce ağ/olmazsa `offline.html`, statik varlıklarda cache-first + arka planda güncelleme
- [x] "Ana ekrana ekle" davranışı doğrulandı: production build + `vite preview` ile test edildi, service worker kaydoldu ve aktif oldu (`scope: .../, active: true`), manifest doğru parse edildi, konsol hatası yok
- Not: İkon `public/icon.svg` — SVG tabanlı (512x512 viewBox, `any` + `maskable` purpose). PNG ikon üretmek için görsel işleme kütüphanesi gerekiyordu (bu ortamda yok); SVG modern Chrome/Android'de yeterli, iOS "ana ekrana ekle" için `apple-touch-icon` olarak da aynı SVG referans verildi (iOS'ta PNG kadar güvenilir olmayabilir — ileride gerçek PNG ikon setiyle değiştirilebilir).
- Service worker sadece production build'de (`import.meta.env.PROD`) kayıt oluyor, dev sunucusunu (HMR) etkilemiyor.

## Faz H — Deploy Hazırlığı (dosya bazlı, gerçek yükleme yok) ✅ (2026-07-30)
- [x] `docker-compose.prod.yml` (prod ortam değişkenleri — placeholder yok, zorunlu; sadece 127.0.0.1'e açık portlar; restart politikaları; log rotasyonu)
- [x] `deploy/nginx.conf.example` — `gamesaloon.fatihdikec.me` için reverse proxy + Socket.io upgrade header'ları + Certbot http-01 location'ı
- [x] `deploy/README.md` — Certbot adımları, `.env.production.example`, VDS'e manuel yükleme adımları (kullanıcı çalıştıracak)
- [x] Kök `README.md`'ye "Prodüksiyon Deploy" bölümü eklendi, "Özellikler" ve güncel proje yapısı ile birlikte tamamen yeniden yazıldı
- `docker compose -f docker-compose.prod.yml config` ile syntax doğrulandı (geçerli)
- **Kapsam hatırlatması**: gerçek VDS'e SSH/yükleme işlemi yapılmadı (kullanıcı kararıyla bu sürüm kapsamı dışında) — sadece hazırlık dosyaları üretildi.

---

## Açık Kararlar / Varsayımlar
| # | Konu | Varsayılan Seçim |
|---|------|-------------------|
| 1 | Oda state'i | Redis (Faz A'dan itibaren aktif kullanılacak — v0.0.1'de sadece bağlantı vardı, şimdi gerçek iş mantığı eklenecek) |
| 2 | Taş görselleri | Görsel varlık (image) kullanılmadan CSS/SVG ile taş çizimi — hız ve bakım kolaylığı için |
| 3 | Sürükle-bırak | Native HTML5 drag-drop (ek kütüphane yok) — v1 için yeterli |
| 4 | Deploy | Sadece hazırlık dosyaları, gerçek VDS işlemi yok (kullanıcı onayı) |
| 5 | Push | Tüm fazlar bitince tek seferde (kullanıcı onayı) |

---

## İlerleme Notları
- 2026-07-30: Plan oluşturuldu ve onaylandı. Kodlamaya başlanıyor.
